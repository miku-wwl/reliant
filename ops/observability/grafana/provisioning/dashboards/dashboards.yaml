apiVersion: 1

providers:
  - name: Reliant
    orgId: 1
    folder: Reliant
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
